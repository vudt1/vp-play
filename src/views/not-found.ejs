<%- include('partials/header') %>

<section class="flex flex-col gap-xl max-w-xl">
  <header class="flex flex-col gap-sm">
    <h1 class="font-display-lg text-display-lg text-on-surface">Không tìm thấy</h1>
    <p class="text-on-surface-variant font-body-lg">
      <%= typeof message !== 'undefined' ? message : 'Trang không tồn tại.' %>
    </p>
  </header>
  <a
    class="inline-flex w-fit bg-primary text-on-primary font-label-sm text-label-sm py-sm px-xl rounded-lg no-underline active:scale-95"
    href="<%= publicPath('/') %>"
  >Về ứng dụng</a>
</section>

<%- include('partials/footer') %>
