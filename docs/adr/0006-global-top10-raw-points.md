# Global top-10 ranking by raw totalPoints

Ranking is a single global board: top 10 Players by raw summed `total_points`. Every Module that awards points writes deltas through the shared settle / `applyPoints` path. No per-module leaderboards, weights, or ELO in MVP. UI and product copy state “tổng điểm mọi trò chơi · top 10”.
